* Numpy
```
np.random.random_sample() // It's common practice in machine learning and data science to generate a matrix with randomly generated values.
size = (1, 2, 3)
print(np.random.random_sample(size=int)) // size 1 = 1D , 2 = 2D ,3 = 3D
print(np.random.random_sample()) // unit 

np.linspace() // range of float 
np.arange(start,stop) // range of int 
```